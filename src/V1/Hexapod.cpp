//
// Created by master on 02.02.20.
//

#include "Hexapod.hpp"

//void Mode_BOOTING_Handler(PE_FSM<ModeState> &fsm);
//void Mode_WAITING_Handler(PE_FSM<ModeState> &fsm);
//void Mode_MOVING_Handler(PE_FSM<ModeState> &fsm);
//void Mode_CONFIGURE_onEntering(PE_FSM<ModeState> &fsm);
//void MODE_SHUTDOWN_onEntering(PE_FSM<ModeState> &fsm);
//void MODE_SHUTDOWN_onDispatch(PE_FSM<ModeState> &fsm);
//
//ModeState MODE_BOOTING   = ModeState(Mode_BOOTING_Handler);
//ModeState MODE_WAITING   = ModeState(Mode_WAITING_Handler);
//ModeState MODE_MOVING    = ModeState().setOnDispatch(Mode_MOVING_Handler);
//ModeState MODE_CONFIGURE = ModeState(Mode_CONFIGURE_onEntering);
//ModeState MODE_SHUTDOWN  = ModeState(MODE_SHUTDOWN_onEntering, MODE_SHUTDOWN_onDispatch);
//
//void Mode_BOOTING_Handler(PE_FSM<ModeState> &fsm) {
//
//}
//
//void Mode_WAITING_Handler(PE_FSM<ModeState> &fsm) {
//
//}
//
//void Mode_MOVING_Handler(PE_FSM<ModeState> &fsm) {
//
//}
//
//void Mode_CONFIGURE_onEntering(PE_FSM<ModeState> &fsm) {
//
//}
//
//void MODE_SHUTDOWN_onEntering(PE_FSM<ModeState> &fsm) {
//
//}
//
//void MODE_SHUTDOWN_onDispatch(PE_FSM<ModeState> &fsm) {
//
//}
